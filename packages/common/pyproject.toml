[tool.poetry]
name = "common"
version = "0.1.0"
description = "Common utilities"
authors = ["CKTT Team <team@cktt.ai>"]
packages = [{include = "common"}]

[tool.poetry.dependencies]
python = "^3.11"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
